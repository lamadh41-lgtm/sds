# قاعدة الاستهلاك الإلزامية — متجر مصران / لوحة الأدمن

أي ميزة أو صفحة أو مستمع أو استعلام **يُنشأ من الآن فصاعداً** يجب أن يُصمَّم وفق هذه القاعدة.
مخالفة القاعدة = إعادة تصميم قبل الدمج.

## 1) لا تقرأ ما لم يتفاعل معه المستخدم
- ممنوع `onSnapshot` / استماع حي على بيانات المستخدم **لم يفتحها** (دردشة مغلقة، تبويب مخفي، قسم أدمن غير مفتوح).
- المستمع يُفعَّل **عند الفتح فقط** ويُوقف فوراً عند الإغلاق أو مغادرة القسم.
- الشارات (badge) إن لزم: استعلام خفيف محدود أو تجميع، لا سحب كل السجلات.

## 2) الشبكة مصدر الحقيقة — المحلي يقلل الحرق
- Firestore = الحقيقة الرسمية.
- `localCache` / `softFetch` / `cachedFetch` للعرض المتكرر في نفس الجلسة.
- أول زيارة: شبكة → كاش. الرجوع/نفس الجلسة: كاش ما دام صالحاً.
- بعد أي كتابة ناجحة على الشبكة: حدّث أو امسح المفتاح المحلي المتعلق فقط.

## 3) حدود القراءات في كل استعلام
- دائماً `limit(...)` مناسب (صفحات 10–20، قوائم إدارية ≤ 80–200 حسب الحاجة).
- فضّل `where` + فهرس على سحب مجموعة كاملة ثم فلترة في المتصفح إن أمكن تجنّب ذلك.
- لا تكرار `getDocs` لنفس البيانات في نفس الشاشة دون داعٍ.

## 4) لا حرق خفي (غير مرئي)
- ممنوع تايمرات/بولنج كل ثوانٍ لبيانات غير ظاهرة.
- ممنوع تحميل صور/ملفات كبيرة قبل ظهورها (lazy إن أمكن).
- ممنوع نسخ مكررة من listeners على نفس المسار.
- Hosting/CDN للملفات الثابتة؛ Firestore للبيانات فقط.

## 5) الكتابات
- اكتب أقل حقول ممكنة (`updateDoc` جزئي لا إعادة كتابة الوثيقة كاملة بلا سبب).
- لا تنشئ وثائق مؤقتة عبثية تُترك في القاعدة.

## 6) عند إضافة ميزة جديدة — قائمة تحقق
- [ ] هل تعمل بدون listener دائم؟
- [ ] هل لها كاش محلي أو softFetch؟
- [ ] هل عليها `limit`؟
- [ ] هل تُوقف عند إخفاء الواجهة؟
- [ ] هل لن تلمس بيانات المستخدم لم يفتحها؟

**الخلاصة:** لا تستهلك سيرفر ولا بيانات ولا قراءات على شيء المستخدم لم يتفاعل معه بعد.
